# xy_rb 整轮训练实验结果

坐标文本特权 + 红框图特权，1 epoch（260 步）On-Policy Self-Distillation。
记录时间 2026-08-26。

---

## 1. 实验配置

| 项 | 值 |
|---|---|
| 变体 | `xy_rb` —— teacher 同时拿到**坐标文本**（`teacher_prompt` 列，system 消息里含 GT 坐标）和**红框图**（`bbox_images` 列） |
| student 看到的 | 干净图 + 干净问题，**零坐标泄漏**（训练日志通路检查已确认） |
| 训练作业 | job `18895386`，tag `vopd-xy_rb-e1` |
| 硬件 | RTX PRO 6000 Blackwell Server Edition ×2，单节点，800G 内存 |
| 耗时 | 17h58m，exit 0，无 traceback / OOM / NCCL 错误 |
| 实际内存峰值 | MaxRSS **400 GiB**（800G 超配一倍，512G 足够） |
| 底模 | `Qwen/Qwen3.5-4B` |
| 数据 | `data/train_xy_rb.parquet`，6241 条 |
| 步数 | 260 = 6241 / 24，恰好 1 epoch |
| loss | `loss_mode=vopd`，广义 JSD `alpha=0.5`，`top_k=-1` |
| teacher | `legacy` 源，EMA 正则，`teacher_update_rate=0.05`，`teacher_always_on=True`。teacher 从不生成，只做 `no_grad` 前向给 student 的 token 打分 |
| batch / 采样 | `train_batch_size=24`，`rollout_n=8`，`max_response_length=1024` |
| 存档 | 每 65 步，得到 `global_step_65/130/195/260` |

**guard 指标全程正确**：`teacher_image_swap_fraction=1.0`、`teacher_always_on_fraction=1.0`、
`policy_fallback_fraction=0.0`、`grpo_fallback_count=0.0`、复读率 0.0%。

**流式留出性质**：rollout dump 写在该 batch 的更新**之前**，且 260 步 = 恰好 1 epoch，
所以每一步的 24 道题在被评估时都是**从未训练过**的。下面所有训练集曲线都是留出集曲线。

---

## 2. 主结果：定位学会了，输出格式塌了

学生输出框 vs 数据集 GT 框，26 步一档（题目级 cluster bootstrap，n≈4990/档）：

| step | 有框率 | IoU 中位 | 95% CI | 准确率 | 尺度错误 |
|---|---|---|---|---|---|
| 1-26 | 99.2% | 0.364 | [0.297, 0.434] | 41.8% | 0.0% |
| 27-52 | 99.6% | 0.508 | [0.456, 0.552] | 38.9% | 0.0% |
| 53-78 | 99.6% | 0.551 | [0.510, 0.589] | 39.9% | 0.1% |
| 79-104 | 99.6% | 0.593 | [0.559, 0.621] | 37.7% | 0.1% |
| 105-130 | 99.0% | 0.621 | [0.587, 0.654] | 35.2% | 0.7% |
| 131-156 | 98.4% | 0.641 | [0.608, 0.671] | 35.8% | 1.4% |
| **157-182** | 94.9% | **0.693** | [0.658, 0.719] | 34.1% | 3.9% |
| 183-208 | 89.2% | 0.642 | [0.586, 0.688] | 34.2% | 6.9% |
| 209-234 | 82.0% | 0.442 | [0.352, 0.517] | 33.5% | 10.2% |
| **235-260** | 78.7% | **0.095** | [0.033, 0.173] | 32.6% | 16.0% |

IoU 在 **step ~170 见顶 0.69，随后崩到 0.095**。

### 崩的是格式，不是定位能力

按输出格式分类重算 IoU：

| 格式 | 峰值 160-179 | 末期 240-260 |
|---|---|---|
| 字典形式 `{"x1":..}` | 38.6%，IoU **0.745** | 33.9%，IoU **0.731** |
| 列表正常 `[x1,y1,x2,y2]` | 52.6%，IoU **0.734** | 27.3%，IoU **0.713** |
| 字典键不全 | 3.8%，IoU 0 | **18.4%**，IoU 0 |
| 完全无框 | 2.8%，IoU 0 | **8.9%**，IoU 0 |
| 0~1 归一化尺度 | 1.1%，IoU 0 | **8.5%**，IoU 0 |
| 0~100 尺度 | 1.0%，IoU 0 | 2.9%，IoU 0 |
| **能解析的合计** | **91.2%** | **61.2%** |

**只要框能解析出来，末期 IoU 仍有 0.73，和峰值持平。** 塌掉的是 JSON schema 依从性。
典型坏输出：

```
{"bbox_2d": 163, "answer": "B"}                       ← 标量
{"bbox_2d": [0.2576, 0.2669, 0.5380, ...]}            ← 0~1 尺度
{"bbox_2d": "625, 245, 708, 525", "answer": "B"}      ← 字符串
{\nbbox_2d": [...], answer: "A"}                      ← 少引号
```

输出长度从 51 token 涨到 120。reward-free 蒸馏里没有任何东西钉住输出格式，
一个 epoch 下来 schema 被侵蚀。

### 分布收窄的签名（小目标层）

| step | IoU=0（完全打偏） | IoU>0.9（几乎完美） |
|---|---|---|
| 1-18 | 16.7% | **8.9%** |
| 73-90 | 11.3% | **2.1%** |

打偏的变少，但接近完美的也从 8.9% 塌到 2.1%。**削两头、往中间挤**——
这是 mode-seeking 自蒸馏的典型特征。值得注意的是，如果增益来自特权，
方向该反过来：teacher 手里有精确到像素的 GT 坐标，应该把高精度那一头**推高**。

---

## 3. Checkpoint 选择

近 10 步窗口统计：

| checkpoint | 合法框率 | 原始 IoU | 合法样本 IoU | 训练集准确率 |
|---|---|---|---|---|
| `global_step_65` | 99.6% | 0.572 | 0.574 | 40.3% |
| `global_step_130` | 97.6% | 0.626 | 0.640 | 35.3% |
| **`global_step_195`** | 81.5% | **0.652** | **0.772** | 36.9% |
| `global_step_260` | 57.8% | 0.000 | 0.726 | 30.4% |

- 评测做格式重试 / 宽松解析 → 用 **195**
- 直接单次采样按原始输出打分 → 用 **130**
- **260 不要用**

四个 checkpoint 已 `chmod -R a-w` 设为只读。恢复可写：
`chmod -R u+w checkpoints/vopd-xy_rb-e1`

> ⚠️ 这批 checkpoint **不能用于 resume 训练**：EMA teacher 不在存档里
> （`fsdp_workers.py:935` 只保存 `actor_module_fsdp`），`resume_mode=auto`
> 会把 teacher 静默重置成 base 模型。它们唯一用途是合并后做推理/评测。

---

## 4. V* 评测

作业 `18921742`，2026-08-26，**B200 单卡，14 分 49 秒**跑完 4 个模型。

**方法**：V* 191 题四选一。vLLM 起服务（训练同款 `perception_chat_template_qwen35.jinja`，
预填空 `<think></think>`），`infer.py` 逐题推理，**`temperature=0` 贪心解码**，
`seed=42`，`max_tokens=4096`，32 并发。判分用仓库 `judge_qwenlm.py` 的
`extract_answer` → `mathruler.grade_answer` → `first_letter_match`，
**`NO_JUDGE=1` 跳过 LLM judge**（原流程里 LLM 只在规则判错时才调用，
只能把错翻成对，所以跳过得到的是**严格下界**）。置信区间为按题配对 bootstrap（5000 次）。

| 模型 | 准确率 | vs base | 配对 Δ 95% CI | direct_attr (115) | rel_pos (76) |
|---|---|---|---|---|---|
| `Vision-OPD-9B`（作者发布的 9B，非本实验） | 90.05% | +10.99 | [+4.2, +17.8] ✔ | 93.0% | 85.5% |
| **`base-qwen35-4b-rerun`**（底模，今天重测） | **79.06%** | — | — | 78.3% | 80.3% |
| `e1-step195` | 75.92% | −3.14 | [−11.5, +5.2] | 73.0% | 80.3% |
| `e1-step130` | 73.82% | −5.24 | [−13.6, +3.1] | 77.4% | 68.4% |
| `vopd-b24-step260`（上一轮训练，旧） | 72.77% | −6.28 | [−15.2, +2.1] | 72.2% | 73.7% |
| **`e1-step260`** | **69.63%** | **−9.42** | **[−17.8, −1.0] ✔** | 61.7% | 81.6% |

**今天实测的是加粗的四个**（base 重测 + step130/195/260），同一条流水线、同一天、
同一判分口径。底模特意重测是因为旧的 base 数字（8/23）用了 LLM judge 且 chat template
未记录，混着比不可靠。

**只有 step260 的回退统计显著**（CI 不含 0）。130 和 195 的 CI 都跨过 0，
在这个基准上和底模分不开。

健全性检查——三个 checkpoint 确实加载了不同权重：

```
base vs 130   0/191 条输出相同     130 vs 195   26/191
base vs 195   0/191                195 vs 260   69/191
base vs 260   0/191
```

训练把输出变简短了：base 长度中位 277 字符（写完整句子），
step130 是 3（`'(A)'`），step195/260 是 1（`'D'`）。

### 4b. 训练格式复测（2×2 判别实验）

作业 `18927765`，2026-08-26，B200，11 分 12 秒。

**动机**：上面 §4 用的是 V* 自带 prompt——查证发现它并非「裸问题」，191/191 条
末尾都带 `"Answer with the option's letter from the given choices directly."`；
而训练时每道题带的是 bbox→answer 的 JSON 指令。也就是说 §4 的评测里
**bbox→answer 通路根本没被触发**，掉分存在另一种解释：评测 OOD，范式没伤模型，
是我们测歪了。本作业把 191 题逐字改写成训练格式（选项 `(A)`→`A.`，
尾部指令替换为从训练 parquet 逐字提取的 JSON 指令；V* 有 84/191 道二选一题），
同样 4 个模型重测，补齐 2×2：

| 模型 | 直接作答指令（§4） | 训练格式 JSON 指令 | 训练格式下 vs base Δ | 95% CI |
|---|---|---|---|---|
| base | 79.06% | **80.10%** | — | — |
| e1-step130 | 73.82% | 76.44% | −3.66 | [−11.5, +4.2] |
| e1-step195 | 75.92% | 70.16% | **−9.95** | [−17.8, −2.1] ✔ |
| e1-step260 | 69.63% | 65.45% | **−14.66** | [−23.6, −6.3] ✔ |

4 个模型在训练格式下 bbox 输出率均 100%、`answer` 字段抽取率 190~191/191——
范式被完整触发，无格式判分伪影。同一模型两种 prompt 的配对差全部不显著
（base +1.05 [−6.8,+8.4]，step130 +2.62，step195 −5.76，step260 −4.19，CI 均含 0）。

**三个判读**：

1. **「评测 OOD」假说被否定。** 在训练自己的主场格式下，trained 模型不仅没有
   恢复到 base，劣势反而更干净：随步数**单调**加深（−3.7 → −10.0 → −14.7），
   step195 也变成显著低于 base。作答能力的损伤是真实、通用、随训练量单调增长的。
2. **推理时的 bbox→answer 范式本身近似中性。** base 加上 JSON 指令只 +1.05
   （CI 含 0）——「先定位帮助作答」这个理论卖点在 V* 上测不出增益，但也无害。
3. **指令格式的影响远小于训练造成的差异。** 格式效应 ±2~6 点全在噪声内，
   模型间差异（base vs step260 达 14.7 点）才是主导。

**分类别（direct_attributes 115 题 / relative_position 76 题）**：

| direct_attributes | base | step130 | step195 | step260 |
|---|---|---|---|---|
| 直接作答指令 | 78.3% | 77.4% (−0.9) | 73.0% (−5.2) | **61.7% (−16.5)** ✔ |
| 训练格式 | 79.1% | 79.1% (±0.0) | 69.6% (−9.6) | **57.4% (−21.7)** ✔ |

| relative_position | base | step130 | step195 | step260 |
|---|---|---|---|---|
| 直接作答指令 | 80.3% | 68.4% (−11.8) | 80.3% (±0.0) | 81.6% (+1.3) |
| 训练格式 | 81.6% | 72.4% (−9.2) | 71.1% (−10.5) | 77.6% (−3.9) |

**损伤几乎全部集中在 direct_attributes**（两种格式下都随步数单调恶化，260 显著），
rel_pos 全部不显著且不单调。direct_attributes 恰是**最接近训练分布**的题型
（定位小目标 + 答属性）——「在最像训练数据的题上被训得最差」，与训练集留出流
42→32.6 的下降是同一现象。这不利于「纯分布偏移」解释（那应该伤离训练分布最远的），
更指向 mode-seeking 收窄挤走了正确答案的概率质量。

**格式效应的同题配对**（训练格式 − 直接作答，n=76/115）：rel_pos 上
base +1.3（翻对 5 / 翻错 4，纯噪声）、step130 +3.9、step195 −9.2 [−19.7,+1.3]
（翻对 6 / 翻错 13）、step260 −3.9（8/11）；direct_attr 上 base +0.9、130 +1.7、
195 −3.5、260 −4.3，全部不显著。三个要点：

- **bbox-then-answer 对任何模型、任何题型都无实测增益**。对 rel_pos 有结构性
  原因：格式只输出**一个** bbox，而空间关系题需要两个物体的位置，单框在结构上
  编码不了关系。单框理论上该帮到的 direct_attr 实测也只有 +0.9~+1.7 的噪声。
- trained 模型在训练格式下总是更差（195 的 rel_pos：直接作答 80.3 = base，
  训练格式 71.1）——**损伤主要住在被训练重塑过的输出模式里**，直接作答指令
  能部分绕开它。
- 实操：下游若只要答案，**用直接作答指令，别触发 JSON 格式**。

---

## 5. 结论

### 5.1 定位能力：确实学会了

合法样本 IoU 从 0.574（step 65）涨到 **0.772**（step 195）。底模零样本是 0.443。

### 5.2 作答能力：确实被伤到了，且不是格式假象

训练集上 `"answer"` 字段的抠出率全程 97.5%–99.3%（格式崩的是 bbox 字段，
answer 字段一直好好的），严格与宽松两种抠法结果几乎一致：

| step | 答案抠出率 | 准确率（严格） | 准确率（宽松） |
|---|---|---|---|
| 1-26 | 97.9% | 41.9% | 42.0% |
| 105-130 | 99.2% | 35.2% | 35.2% |
| 157-182 | 99.2% | 34.2% | 34.2% |
| 235-260 | 97.5% | 32.4% | 32.6% |

**42.0% → 32.6% 是真实的作答能力下降。**

### 5.3 但归因到「先生成 bbox 再作答」站不住

三条反证：

1. **V\* 上一个 bbox 都没生成**（191 条输出全是裸字母，`含bbox字样=0/191`），
   伤害却一样大：V\* 掉 **−9.4 点**（79.06→69.63），训练集掉 **−9.4 点**（42.0→32.6）。
   两个数字一致，一个有 bbox 一个没有 → 指向与输出格式无关的通用能力回退。
2. **范式里没有 "reason" 这一步**：chat template 预填空 `<think></think>`，
   强制跳过思维链。实际是 bbox → 答案字母，中间没有推理。
3. **下降从第 1 步就开始且匀速**，而 bbox 格式要到 step 170 之后才崩，
   两件事时间上对不上。

> 2026-08-26 追加：§4b 的 2×2 复测直接检验并**否定**了剩下的替代解释
> 「评测 OOD」。同时 base + JSON 指令仅 +1.05（噪声内）——推理时先输出 bbox
> 近似中性，不助也不害。伤害来自**训练过程**，与推理时是否触发 bbox 无关。

### 5.4 真正原因未确定

至少三个候选，现有数据分不开：

1. 输出里带 bbox 这件事本身
2. OPSD/EMA 自蒸馏目标本身（reward-free、mode-seeking；已观察到方差收窄签名）
3. 一个 epoch 的 grounding 数据把模型从通用 VQA 拉走（纯数据分布偏移）

### 5.5 增益来源同样未确定

底模 per-question：中位 0.443，worst-of-8 0.023，**best-of-8 0.860**；
训练后 per-question 中位 0.668；teacher 探针 0.803。

因为 0.668 < 0.860，**纯自蒸馏的 mode-sharpening 在原则上足以解释全部增益**，
特权是否真的起了作用**没有定论**。（这个上界是宽松的——best-of-8 是用 GT 挑出来的，
自蒸馏实际到不了。）

---

## 6. 未解问题与下一步

### 待跑的对照组（决定性实验）

| 对照组 | 改法 | 能回答什么 |
|---|---|---|
| **纯自蒸馏** | teacher 无 `teacher_prompt`、用同样的干净图。`25_prep_data.py` 的 `VARIANTS` 加 `"none": (False, "clean")`，`26_run_train.sh` 加 `WANT_TP=0; WANT_DIFF=0` 分支 | 增益里特权占多少 vs 自锐化占多少（§5.5） |
| **只答题** | 输出格式改成只有 `{"answer": "X"}`，不要 bbox。数据准备里改格式指令即可，loss 不动 | 作答退化是不是 bbox 造成的（§5.4 的候选 1） |

节点有 8 张卡、本次只用了 2 张，两个对照组可并行挂，不增加墙钟时间。

### 已知的坑（记录给下次）

- **训练日志的 IoU 列会骗人**：它只在格式合法的样本上算，末期还显示 0.3~0.6，
  真实（不合法记 0）是 0.095。下次给日志加一列原始 IoU。
- **`eval/model_answer/*.jsonl` 的字段**：`response` 是**标准答案**
  （`infer.py` 把 benchmark 原记录整条抄过来），模型输出在 **`model_answer`**。
  看错会算出 100% 或莫名其妙的数。
- **别自己写首字母正则**（`"tissue box"` 里的 `b` 会被抠成选项 B），
  用 `16_score_norule.py` 或 import `judge_qwenlm.py` 的判分函数。
- **卡型限制**：训练锁死 RTX PRO 6000（sm_120）——verl 直接
  `from flash_attn import ...`，而 `flash_attn_2_cuda.so` 只有 sm_120 cubin、不带 PTX。
  但**评测可以换任意卡**——vLLM 在 CUDA 分支走自带的 `vllm.vllm_flash_attn`
  （`fa_utils.py:19-23`），外部 flash_attn 只在 ROCm 分支 import。
  本次评测在 B200 上跑通即为验证。
- **内存**：训练 800G 超配，实际峰值 400 GiB，512G 足够。
- **`scripts/merge_checkpoint.sh` 是原地合并**，第 21 行有
  `find "${BASE_DIR}" ... -delete`，会删掉 checkpoint 目录顶层文件。
  应直接调 `verl.model_merger` 并把 `--target_dir` 指向独立目录。

### 结论的强度限制

V\* 只有 191 题，配对 CI 宽达 ±9 个点——「零代价」和「5 点代价」在这份数据上
区分不了。同一底模两次评测差 1.57 点（8/23 的 80.63% vs 今天的 79.06%，
不过配置不完全相同）。

~~要把「step195 无代价」坐实需要更大的基准。~~ **§4b 已推翻「step195 无代价」**：
训练格式下 step195 显著低于 base（−9.95，CI 不含 0）。目前**作答代价仍在噪声内的
只有 step130**（两种格式下均不显著）；step195 定位最强（IoU 0.772）但作答已掉约
10 点。grounding-质量 与 作答能力 的取舍在 130 和 195 之间，用哪个取决于下游任务
更看重哪头。更大的基准仍然需要——130 的「不显著」同样可能只是检验力不足。
`eval/run_eval.sh` 支持 mmstar / cv-bench / mmvp / hrbench-4k/8k / pope，
但本地只有 V\* 的数据，其余需先下载。

---

## 7. 后续实验：多框变体 xy_rb_mb（2026-08-27 完成）

> **独立成文**：多框实验的完整设置（与基础 Vision-OPD 的特权/prompt 差异对照表、
> prompt 原文）与官方 judge 口径的分类别结果已单独整理在 **`multibox_experiment.md`**，
> 那份是引用用的最终版；本节保留过程记录（含 NO_JUDGE 口径的中间数字，已被 §7.4 修订）。

放开单框限制的对照轮：特权与 xy_rb 完全相同，仅输出格式换成
`{"objects": [{"bbox_2d": [...], "label": ...}, ...], "answer": ...}`（FMT_MB，
近 Qwen 原生 grounding schema，允许 1+ 框）。训练 job 18928899，260 步 =1 epoch，
COMPLETED 17h41m；checkpoint 65/130/195/260（已设只读）。

### 7.1 训练分布（多框口径重扫 rollout，10-step 桶 ×1920 样本）

| 步数 | 自由文本 | answer存在 | 条件准确率 | 严格MB合规 | maxIoU | 平均框数 |
|---|---|---|---|---|---|---|
| base探针 | 11.9% | ~88% | ~40% | 60.4% | 0.502 | ~1.5 |
| 1-10 | 0.8% | 76.7% | 45.8% | 59.9% | 0.442 | 2.66 |
| 61-70 | 1.1% | 96.4% | 42.9% | **93.0%**(峰) | 0.463 | 1.22 |
| 181-190 | 5.6% | 87.2% | 37.6% | 44.4% | **0.627**(峰) | 1.00 |
| 251-260 | 17.4% | 74.0% | 36.5% | 15.5% | 0.090 | 0.80 |

- **多框自由没被使用**：框数均值 50 步内收敛回 ~1.2，末期 0.8（两成样本无框）。
- **格式漂移复发，时间表与单框几乎相同**（~110 步起漂、190 后崩）→
  漂移是 reward-free JSD 训练的内生现象，与具体 bbox 格式无关。
- **答案通道伤得比单框更重**：answer 存在率 96.5%→74%（e1 恒 97.5-99.3%）；
  条件准确率（仅看有 answer 的）从 ~44% 降到 ~36%，全样本准确率的
  早期"上涨"只是 answer 存在率恢复的假象。
- ⚠️ 训练日志自带的 28 号分析表对 _mb 变体口径错误（查的是单框 schema），
  其「格式合法」列不可用。
- maxIoU 峰 0.627 < 单框的 0.69-0.75，定位蒸馏也没变好。

### 7.2 V\*（job 18955787；NO_JUDGE 严格下界，与 §4 同一把尺）

只测 65/130（195/260 格式已崩，只会复现单调伤害曲线）。「训练格式」
= FMT_MB 指令版 vstar（逐字取自 mb parquet；base 参考格用的是它自己的单框指令结果）。

| 模型 | 裸 prompt | mb 训练格式 | 格式格备注 |
|---|---|---|---|
| base | 79.06 | **47.12** | 无 answer 字段 70/191，平均 13.5 框，rel_pos 崩到 17.1% |
| mb-step65 | **79.06** | **83.25** | 0 缺失，1.4 框，direct 85.2 / rel_pos 80.3 |
| mb-step130 | 77.49 | 74.87 | 0 缺失 |
| e1-step130(参考) | 73.82 | 76.44(单框指令) | |

配对 bootstrap（5000 次，n=191）：

| 对比 | Δ | 95% CI | 判定 |
|---|---|---|---|
| mb-65 − base（裸） | ±0.00 | [−5.24, +5.24] | ns：**step 65 零损伤** |
| mb-65-fmt − base（裸） | +4.19 | [+0.00, +8.90] | ns，CI 下界恰为 0（压线） |
| mb-130 − e1-130（裸，同位置） | +3.66 | [−0.52, +7.85] | ns（压线）：多框方向占优 |
| mb-130-fmt − e1-130-fmt | −1.57 | [−6.81, +3.66] | ns |
| **mb-65-fmt − mb-130-fmt** | **+8.38** | [+3.66, +13.09] | **✔显著：65→130 之间伤害已发生** |

### 7.3 结论

1. **step 65 是全部实验里第一个「零损伤 + 格式内占优」的 checkpoint**：
   裸 prompt 与 base 逐题持平，训练格式下 83.25%（高于 base 任何格子，压线不显著）。
   训练把 base 完全驾驭不了的多框指令（47.12%）教成了满分格式掌握。
2. **伤害在 65→130 步之间就已显著发生**（格式内 −8.38 ✔），早于严格合规率的
   可见崩塌——callback 到 §5：伤害机制不在格式，在蒸馏目标本身。
3. 多框 vs 单框：同位置（130）裸 prompt +3.66（压线 ns），格式内基本持平——
   多框**方向上减缓**了伤害，但 n=191 不足以盖章。
4. ⚠️ 混淆提醒：「mb-step65 比 e1-step130 高很多」混着步数变量；
   单框的 step 65 checkpoint 存着未测，补测即可解耦（32/33 号 STEPS=65）。
5. 下一步含义：既然 65 步就拿到零损伤+格式掌握，**早停（~65 步）+ 答案 token
   蒸馏/crop 特权**（§6 表）是最有希望的组合；样本量问题需要更大 benchmark。

复现：`sbatch 35_eval_mb.sbatch`（合并+双格式评测+2×2 汇总一次跑完）；
评测日志 `Vision-OPD-setup/logs/evalmb-18955787.log`；
rollout 多框口径分析脚本见该日志同目录（analyze_mb.py 的逻辑已记录于 §7.1 表注）。

### 7.4 官方 judge 口径（gpt-oss-120b，job 18956788）——修订 §7.3 的结论

用论文同款 judge（README:65 指定 `openai/gpt-oss-120b`）+ 仓库原版
`judge_qwenlm.py`/`cal_acc.py` 把全部 17 份 vstar 结果统一复判
（判卷不重跑推理；旧自判结果备份在 `eval/judge/vstar_selfjudge_backup_20260827`）。

**① 论文基线复现成功**：base 裸 prompt = **83.25%**，论文 84.29% 只差 2 题
（159 vs 161/191），在同模型两次重跑的抖动（3 题）之内。
之前差 5.2 个点的主因就是 NO_JUDGE。

**② NO_JUDGE 下界对模型不是中性的（新坑，重要）**：judge 只救了 base
（79.06→83.25，40 条自由文本里翻对 8 条），**几乎没救任何训练后的模型**（±0.5 内）
——训练后输出干净字母/JSON，规则全能判，没有翻盘空间。
→ **NO_JUDGE 口径系统性低估了训练伤害**，§4/§7.2 的所有 Δ 都偏乐观约 4 个点。

**③ 官方口径的修订判定**（配对 bootstrap 5000 次，对照 base 裸 83.25）：

| 对比 | NO_JUDGE 口径 | 官方 judge 口径 | 判定变化 |
|---|---|---|---|
| mb-65 裸 − base | ±0.00 ns | **−4.19** [−8.90,+0.52] ns | "零损伤"降级为"压线负" |
| mb-65-fmt − base | +4.19 压线 | **±0.00** [−3.66,+3.66] ns | "占优"降级为"持平" |
| mb-130 裸 − base | −1.6 ns | **−5.76 ✘** | 变显著 |
| mb-130-fmt − base | −4.2 ns | **−7.85 ✘** | 变显著 |
| e1-130 裸 − base | −5.2 ns | **−9.42 ✘** | 变显著 |
| e1-130-fmt − base | −2.6 ns | **−6.81 ✘** | 变显著 |
| mb-130 − e1-130（裸） | +3.66 ns | +3.66 ns | 不变 |
| mb-65-fmt − mb-130-fmt | +8.38 ✔ | **+7.85 ✔** | 不变 |

分类别也同样收缩：base direct_attributes 被 judge 抬到 84.3，
§7.2 里 mb-65-fmt 的"+7.0✔"缩成 +0.9 ns——**全项目不再有任何显著为正的格子**。

**④ 修订后的§7.3**：多框实验的可守结论只剩四条——
(a) base 复现 83.25≈论文 84.29；
(b) step 65 是"无显著伤害 + 格式内持平"，不是增益；
(c) 130 步时两种变体伤害都已显著（mb −5.76 / e1 −9.42），多框方向性更轻（+3.66 ns）；
(d) 伤害在 65→130 之间显著发生（−7.85 ✔），早于格式崩塌。
对下一步的含义不变且更迫切：早停 + 换蒸馏目标（答案 token / crop 特权），
因为现口径下**现有配方连持平都只在 65 步勉强守住**。

---

## 附：复现命令

```bash
# 训练
cd /sfs/weka/scratch/nkw3mr/Vision-OPD-setup
VARIANT=xy_rb STEPS=260 TAG=vopd-xy_rb-e1 SAVE_FREQ=65 sbatch 31_train_epoch.sbatch

# 分析 rollout（IoU / 格式）
python 28_analyze_rollouts.py \
    /sfs/weka/scratch/nkw3mr/Vision-OPD/rollouts/vopd-xy_rb-e1 \
    /sfs/weka/scratch/nkw3mr/Vision-OPD/data/train_xy_rb.parquet

# 合并 + V* 评测（4 个模型一次跑完）
sbatch 32_eval_ckpts.sbatch          # 默认 STEPS="130 195 260"，B200 单卡约 15 分钟

# 单独重算某个模型的分数
python 16_score_norule.py --benchmark vstar --model e1-step195_seed42 --by-category
```

相关文件：
- 训练日志 `Vision-OPD-setup/logs/vopd-18895386.log`
- 评测日志 `Vision-OPD-setup/logs/eval-18921742.log`
- rollout `Vision-OPD/rollouts/vopd-xy_rb-e1/{1..260}.jsonl`
- 合并权重 `Vision-OPD/merged/vopd-xy_rb-e1-step{130,195,260}/`
- 推理产物 `Vision-OPD/eval/model_answer/vstar/*_seed42_answer.jsonl`
